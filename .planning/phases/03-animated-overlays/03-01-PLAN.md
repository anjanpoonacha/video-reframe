---
phase: 03-animated-overlays
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/brand-kit.ts, src/templates.ts]
autonomous: true
requirements: [OVRL-01, OVRL-02, OVRL-03, OVRL-04, TMPL-03]
must_haves:
  truths:
    - "Exported video starts with 2.5s animated intro bumper (logo scale-up + channel name fade-in + hold + fade-out)"
    - "Lower third shows channel name AND handle (second line, lighter, smaller) animating in after intro ends"
    - "Kinetic text 'PREMIUM CONTENT' appears at 3s with slide-up entrance and slide-down exit"
    - "Logo watermark at 25% opacity appears on every frame from 2.5s onward at chosen corner"
    - "All overlays are bundled as one 'Pro Pack' template in the registry"
  artifacts:
    - path: "src/templates.ts"
      provides: "proPackTemplate definition with composite render function"
      contains: "proPackTemplate"
    - path: "src/brand-kit.ts"
      provides: "BrandKit interface with channelHandle field"
      contains: "channelHandle"
  key_links:
    - from: "src/templates.ts"
      to: "TEMPLATE_REGISTRY"
      via: "proPackTemplate registered"
      pattern: "TEMPLATE_REGISTRY.*proPackTemplate"
    - from: "src/templates.ts"
      to: "src/brand-kit.ts"
      via: "loadBrandKit() imports BrandKit with channelHandle"
      pattern: "brandKit\\.channelHandle"
---

<objective>
Create the "Pro Pack" composite overlay template that bundles animated intro bumper, enhanced lower-third with handle, kinetic text, and persistent watermark into a single OverlayRenderFn.

Purpose: Deliver the core professional branded overlay — all 4 animated overlay types (OVRL-01 through OVRL-04) plus the cohesive template package (TMPL-03) in one vertical slice.
Output: Working Pro Pack template registered in TEMPLATE_REGISTRY that produces broadcast-quality branded overlay on every exported frame.
</objective>

<execution_context>
@/Users/i548399/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/i548399/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-animated-overlays/03-CONTEXT.md
@src/templates.ts
@src/brand-kit.ts
@src/overlay.ts

<interfaces>
From src/overlay.ts:
```typescript
export type OverlayRenderFn = (
  ctx: CanvasRenderingContext2D,
  time: number,
  width: number,
  height: number,
) => void;
```

From src/templates.ts:
```typescript
export interface TemplateInstance {
  render: OverlayRenderFn;
  dispose(): void;
}

export interface TemplateDefinition {
  name: string;
  duration: number;
  create(brandKit: BrandKit): Promise<TemplateInstance>;
}

export const TEMPLATE_REGISTRY: TemplateDefinition[];
export async function getActiveTemplate(): Promise<TemplateInstance>;
```

From src/brand-kit.ts:
```typescript
export interface BrandKit {
  channelName: string;
  logo: string | null;
  primaryColor: string;
  accentColor: string;
  logoPosition: "TL" | "TR" | "BL" | "BR";
}

export function loadBrandKit(): BrandKit;
export function saveBrandKit(kit: BrandKit): boolean;
```
</interfaces>
</context>

## Phase Goal

**As a** content creator, **I want to** export branded shorts with animated intro, lower third, kinetic text, and watermark, **so that** my exports look professionally produced without manual editing.

<tasks>

<task type="auto">
  <name>Task 1: Add channelHandle to BrandKit and create Pro Pack composite overlay</name>
  <read_first>src/brand-kit.ts, src/templates.ts, src/overlay.ts, .planning/phases/03-animated-overlays/03-CONTEXT.md</read_first>
  <files>src/brand-kit.ts, src/templates.ts</files>
  <action>
  TWO changes to src/brand-kit.ts:
  1. Add `channelHandle: string` to the BrandKit interface (per D-10).
  2. Add `channelHandle: ""` to DEFAULT_BRAND_KIT (empty string = second line hidden per D-10).

  Then in src/templates.ts, create `proPackTemplate: TemplateDefinition` with these specifications:

  NAME: "Pro Pack"
  DURATION: Infinity (composite manages its own timing — watermark is persistent)

  The `create(brandKit)` function:
  - Decode logo to ImageBitmap using the existing `decodeLogo` helper (reuse pattern from lowerThirdTemplate).
  - Build ONE paused GSAP timeline that animates a state object with these phases:

  STATE OBJECT FIELDS:
  - introLogoScale (0.8 → 1.0), introLogoAlpha (0 → 1 → 0), introNameAlpha (0 → 1 → 0), introVignetteAlpha (0 → 1 → 0)
  - lowerBarWidth (0 → 1), lowerPlateAlpha (0 → 1), lowerTextAlpha (0 → 1), lowerHandleAlpha (0 → 1), lowerMasterAlpha (1 → 0)
  - kineticY (30 → 0 → 30), kineticAlpha (0 → 1 → 0)
  - watermarkAlpha (0 → 1) — snaps to 1 at 2.5s

  TIMELINE KEYFRAMES (per D-05, D-06, D-07, D-08, D-11, D-14, D-15, D-16, D-18):
  - 0.0s: Intro starts — logo scales 80% → 100% with fade-in over 0.6s (ease: power2.out)
  - 0.6s: Channel name fades in below logo over 0.4s (ease: power2.out)
  - 1.0s: Hold (no animation change)
  - 2.0s: Everything fades out over 0.5s (ease: power2.inOut)
  - 2.5s: Watermark snaps to alpha=1 (instant, per D-18 "no animation"), lower-third accent bar begins
  - 2.5s: Lower-third entrance — reuse Phase 2 lower-third animation pattern: accent bar expands (0.25s power3.out), plate slides in (0.35s power2.out), text fades (0.3s), handle text fades (0.3s, 0.05s after name)
  - 5.5s: Lower-third masterAlpha fades to 0 over 0.8s (power2.inOut) — 3s hold per D-11
  - 3.0s: Kinetic text slides up 30px over 0.3s (power2.out), alpha 0→1
  - 3.3s: Kinetic hold (2s)
  - 5.3s: Kinetic slides down 30px + alpha fade over 0.4s (power2.inOut)

  RENDER FUNCTION (the returned OverlayRenderFn):
  Called per-frame with (ctx, time, w, h). Logic:
  1. `tl.seek(time)` — advance GSAP to current video time
  2. Scale factor: `scale = w / 1080`
  3. INTRO BUMPER (only renders if time < 2.5s per D-08):
     - Dark vignette: radial gradient from transparent center to rgba(0,0,0,0.3) at edges, alpha = introVignetteAlpha (per D-07)
     - Logo centered: size = 15% of canvas width, centered vertically and horizontally within safe zone. If logoBitmap exists, draw circular-clipped; else use drawDefaultLogo. Alpha = introLogoAlpha, scale = introLogoScale (per D-05, D-06)
     - Channel name centered below logo with 12px*scale gap. Font: `bold ${Math.round(w*0.04)}px -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif`. Color white. Alpha = introNameAlpha (per D-03, D-06)
  4. WATERMARK (renders if time >= 2.5s per D-16):
     - Use getLogoPosition for corner placement. Size = 5% of canvas width (per D-17).
     - Alpha = watermarkAlpha * 0.25 (total visible opacity 25% per D-17).
     - Draw logoBitmap circular-clipped, or drawDefaultLogo if no logo.
  5. LOWER THIRD (renders if time >= 2.5s AND time < 6.3s):
     - Reuse exact Phase 2 lower-third rendering logic (accent bar, frosted plate, primary edge, text) from lowerThirdTemplate.
     - ADD second line: handle text below channel name. Font: `500 ${Math.round(14*scale)}px` same font stack. Color white. Alpha = lowerHandleAlpha * 0.6 (per D-09). Only draw if brandKit.channelHandle is non-empty (per D-10).
     - Apply lowerMasterAlpha as master multiplier on all lower-third drawing ops.
  6. KINETIC TEXT (renders if time >= 3.0s AND time < 5.7s per D-14):
     - Text "PREMIUM CONTENT" (hardcoded per D-15).
     - Font: `bold ${Math.round(h*0.08)}px` same font stack (per D-13).
     - Centered horizontally via measureText. Vertical center of frame + kineticY offset (per D-13).
     - Drop shadow: ctx.shadowOffsetX=2, ctx.shadowOffsetY=2, ctx.shadowColor="rgba(0,0,0,0.5)" (per D-13).
     - Alpha = kineticAlpha.
     - Color white (per D-04, D-13).

  DISPOSE: Kill GSAP timeline, close logoBitmap.

  REGISTRY: Add proPackTemplate to TEMPLATE_REGISTRY array (push after lowerThirdTemplate). Update `getActiveTemplate()` to return proPackTemplate instead of lowerThirdTemplate (per D-24 — Pro Pack is new default). Keep lowerThirdTemplate as standalone lighter option per D-24.

  EASING RULES (per D-01, D-02): Only use power2.out, power3.out for entrances. power2.inOut for exits. NEVER bounce or elastic. One exception: the lower-third logo uses back.out(1.4) from Phase 2 — preserve that specific animation.
  </action>
  <verify>
    <automated>cd /Users/i548399/SAPDevelop/github.com/personal/video-reframe.git/main && bun build ./index.html --outdir ./dist 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
  - src/brand-kit.ts BrandKit interface includes `channelHandle: string`
  - src/brand-kit.ts DEFAULT_BRAND_KIT includes `channelHandle: ""`
  - src/templates.ts exports proPackTemplate with name "Pro Pack"
  - proPackTemplate.create() returns a TemplateInstance with render and dispose
  - render function handles intro (0-2.5s), watermark (2.5s+), lower-third (2.5-6.3s), kinetic text (3-5.7s)
  - TEMPLATE_REGISTRY contains both lowerThirdTemplate and proPackTemplate
  - getActiveTemplate() returns proPackTemplate.create(brandKit) — Pro Pack is default
  - All easings are power2.out, power3.out, power2.inOut, or back.out(1.4) only — no bounce/elastic
  - Watermark drawn at 25% opacity, 5% canvas width
  - Intro logo at 15% canvas width, centered in safe zone
  - Kinetic text at 8% canvas height font size
  - Build succeeds with no TypeScript errors
  </acceptance_criteria>
  <done>Pro Pack template registered as default, build passes, composite render function handles all 4 overlay types with correct timing per decisions D-05 through D-18, D-22, D-23, D-24</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → BrandKit | User-controlled string data flows into canvas text rendering |
| Brand logo dataURL → ImageBitmap | User-uploaded image decoded and drawn per-frame |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | channelHandle field | accept | String rendered via ctx.fillText which auto-escapes — no injection vector in Canvas2D |
| T-03-02 | Denial of Service | GSAP timeline memory | mitigate | dispose() kills timeline and closes ImageBitmap — same pattern as Phase 2 lowerThirdTemplate |
| T-03-03 | Information Disclosure | Brand data in localStorage | accept | Local-only app, no network transmission, user's own device |
</threat_model>

<verification>
- `bun build ./index.html --outdir ./dist` completes without errors
- grep confirms proPackTemplate exists in templates.ts
- grep confirms channelHandle in BrandKit interface
- getActiveTemplate returns Pro Pack (not lowerThirdTemplate)
</verification>

<success_criteria>
Exported video renders all 4 overlay components (intro bumper, lower third with handle, kinetic text, watermark) with correct timing. Pro Pack is the default template. Build passes.
</success_criteria>

<output>
Create `.planning/phases/03-animated-overlays/03-01-SUMMARY.md` when done
</output>
